This is an initial version which simply 
  * detects a peak at around 80MHz (FM signal)
  * calculates the frequency offset from the detected peak
  * adjust the ppm offset of the RTL-SDR dongle
  